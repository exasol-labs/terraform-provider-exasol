---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "exasol_impersonation_grant Resource - terraform-provider-exasol"
subcategory: ""
description: |-
  Grants a user or role permission to impersonate one regular database user.
---

# exasol_impersonation_grant (Resource)

Grants a user or role permission to impersonate one regular database user.

## Example Usage

```terraform
locals {
  regular_users = [
    "ALICE@example.com",
    "BOB@example.com",
  ]
}

# One Terraform resource is created for each regular user.
resource "exasol_impersonation_grant" "mcp" {
  for_each = toset(local.regular_users)

  grantee           = "MCP_ROLE"
  impersonated_user = each.value
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `grantee` (String) User or role receiving the impersonation permission.
- `impersonated_user` (String) Regular database user whose identity may be impersonated. Roles are not accepted.

### Read-Only

- `id` (String) Terraform ID in format: GRANTEE|IMPERSONATED_USER

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/bin/sh

# Format: GRANTEE|IMPERSONATED_USER
terraform import exasol_impersonation_grant.mcp 'MCP_ROLE|ALICE@EXAMPLE.COM'
```
