---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "exasol_system_privilege Resource - terraform-provider-exasol"
subcategory: ""
description: |-
  Grants system-level privileges to users or roles. System privileges include CREATE SESSION, CREATE TABLE, CREATE SCHEMA, USE ANY SCHEMA, etc.
---

# exasol_system_privilege (Resource)

Grants system-level privileges to users or roles. System privileges include CREATE SESSION, CREATE TABLE, CREATE SCHEMA, USE ANY SCHEMA, etc.

## Example Usage

```terraform
resource "exasol_system_privilege" "create_session" {
  grantee   = exasol_user.analyst.name
  privilege = "CREATE SESSION"
}

# With ADMIN OPTION (grantee can grant this privilege to others)
resource "exasol_system_privilege" "create_schema" {
  grantee           = exasol_role.etl_role.name
  privilege         = "CREATE SCHEMA"
  with_admin_option = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `grantee` (String) User or role name receiving the privilege.
- `privilege` (String) System privilege name (e.g., 'CREATE SESSION', 'CREATE TABLE', 'USE ANY SCHEMA').

### Optional

- `with_admin_option` (Boolean) Grant the privilege with ADMIN OPTION, allowing the grantee to grant this privilege to others.

### Read-Only

- `id` (String) Terraform ID in format: GRANTEE|PRIVILEGE|ADMIN_OPTION

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Format: GRANTEE|PRIVILEGE|ADMIN_OPTION
terraform import exasol_system_privilege.create_session 'ANALYST_USER|CREATE SESSION|false'
```
