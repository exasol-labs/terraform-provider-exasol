---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "exasol_role_impersonation_grant Resource - terraform-provider-exasol"
subcategory: ""
description: |-
  Grants a user or role permission to impersonate one database role.
---

# exasol_role_impersonation_grant (Resource)

Grants a user or role permission to impersonate one database role.

## Example Usage

```terraform
locals {
  impersonated_roles = [
    "TECHNICAL_DBA_ROLE",
    "REPORTING_ROLE",
  ]
}

resource "exasol_role_impersonation_grant" "mcp" {
  for_each = toset(local.impersonated_roles)

  grantee = "MCP_ROLE"
  target  = each.value
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `grantee` (String) User or role receiving the impersonation permission.
- `target` (String) Role whose members may be impersonated. Every user holding this role becomes impersonable.

### Read-Only

- `id` (String) Terraform ID in format: GRANTEE|TARGET

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/bin/sh

# Format: GRANTEE|TARGET
terraform import exasol_role_impersonation_grant.mcp 'MCP_ROLE|TECHNICAL_DBA_ROLE'
```
