---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "exasol_user_impersonation_grant Resource - terraform-provider-exasol"
subcategory: ""
description: |-
  Grants a user or role permission to impersonate one database user.
---

# exasol_user_impersonation_grant (Resource)

Grants a user or role permission to impersonate one database user.

## Example Usage

```terraform
locals {
  regular_users = [
    "ALICE@example.com",
    "BOB@example.com",
  ]
}

resource "exasol_user_impersonation_grant" "mcp" {
  for_each = toset(local.regular_users)

  grantee = "MCP_ROLE"
  target  = each.value
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `grantee` (String) User or role receiving the impersonation permission.
- `target` (String) Database user whose identity may be impersonated.

### Read-Only

- `id` (String) Terraform ID in format: GRANTEE|TARGET

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/bin/sh

# Format: GRANTEE|TARGET
terraform import exasol_user_impersonation_grant.mcp 'MCP_ROLE|ALICE@EXAMPLE.COM'
```
